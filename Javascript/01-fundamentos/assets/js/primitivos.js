// Strings '' "" ``
let names = 'César Uriel';
let midleNames = 'Alvarez Rojas';

// booleans true false
let isMarvel = true;

// numbers
let age = 23;
let age2 = 23.6;

// undefined
let superPower;

// Object
let imNull = null;

// Symbols, there r not the same
let imSymbol = Symbol('a');
let imSymbol2 = Symbol('a');

console.table({names, midleNames, isMarvel, age, age2, superPower, imNull, imSymbol, imSymbol2});
